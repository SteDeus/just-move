import { StyleSheet, View } from "react-native";
import RoomData from "../Components/Pages/RoomData";

export default function HomePage() {

  

  return (
    <View >
        <RoomData />
    </View>
  );
}

const styles = StyleSheet.create({
});
